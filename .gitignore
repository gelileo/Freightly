__pycache__/
*.pyc
.pytest_cache/
cases/
.DS_Store
.env
.env.*
.venv/
.playwright-mcp/

hs.db
*.db
.vscode/
.run/
# raw email corpus — dev-only dataset, not in git (see tests/fixtures for committed samples)
LTL-MAIL*

# design docs / guides — contain PII (emails, addresses, BOLs); kept locally, not in git
docs/
